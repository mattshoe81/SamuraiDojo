<div class="container" ng-controller="MyProfileController as vm">

    <div class="spacer-25"></div>

    <div class="row">
        <h3 class="underline">Your Stats:</h3>
        <div class="col-sm-6">
            <table class="table table-condensed">
                <tbody>
                    <tr>
                        <td class="bold">Username:</td>
                        <td class="lowercase">{{vm.Player.Name}}</td>
                    </tr>
                    <tr>
                        <td class="bold">Total Points:</td>
                        <td>{{vm.Player.TotalPoints}}</td>
                    </tr>
                    <tr>
                        <td class="bold">Rank:</td>
                        <td>{{vm.Player.Rank}}</td>
                    </tr>
                    <tr>
                        <td class="bold">Battles As Sensei:</td>
                        <td>{{vm.GetSenseiCount(vm.Player, vm.Battles)}}</td>
                    </tr>
                    <tr>
                        <td class="bold">Battles Fought:</td>
                        <td>{{vm.Player.BattlesCompleted}}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>


    <div class="row">

        <h3 class="underline">Your History:</h3>
        <div class="spacer-10"></div>
        <div>
            <div class="row">
            <div class="col-sm-6">
                <table class="table table-condensed">
                    <thead>
                        <tr>
                            <th>Battle</th>
                            <th>Points</th>
                            <th>Deadline</th>
                            <th>Sensei</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr ng-repeat="battle in vm.Battles">
                            <td>{{battle.Battle.Name}}</td>
                            <td>{{vm.GetPointsFromBattle(battle, vm.Player)}}</td>
                            <td>{{battle.Battle.Deadline | date:'EEEE, MM/dd/yyyy'}}</td>
                            <td>{{vm.IsSensei(battle, vm.Player) ? 'Yes' : 'No'}}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            </div>
        </div>

    </div>

</div>